/*
//2-D matrix-to-find-sum-and-product-of-all-elements
#include <stdio.h>
main()
{
	int r,c;
	int i,j,sum=0,prod=1;
	scanf("%d",&r);
	scanf("%d",&c);
	int arr[r][c];  //declaraion
	for(i=0;i<r;i++)
	{
		for(j=0;j<c;j++)
		{
			scanf("%d",&arr[i][j]);
		}
	}
	// finding sum, product
	for(i=0;i<r;i++)
	{
		for(j=0;j<c;j++)
		{
			sum=sum+arr[i][j];
			prod=prod*arr[i][j];
		}
	}
	printf("\nSum = %d\nProduct=%d",sum,prod);
}
*/
